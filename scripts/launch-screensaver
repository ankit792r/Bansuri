#!/usr/bin/env bash

# Kill any existing screensaver instance
pkill -f "kitty.*bansuri.screensaver" 2>/dev/null

# Launch kitty fullscreen with opacity and cbonsai
exec kitty \
	--app-id bansuri.screensaver \
	--override background_opacity=0.8 \
	--title screensaver \
	sh -c 'cbonsai -li'
